# create a private key + self-signed cert (valid for 1 year)
sudo openssl req -x509 -newkey rsa:4096 -keyout /etc/ssl/private/ollama-selfsigned.key -out /etc/ssl/certs/ollama-selfsigned.crt -sha256 -days 365 -nodes
